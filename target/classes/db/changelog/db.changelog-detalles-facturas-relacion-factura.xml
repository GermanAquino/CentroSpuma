<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="21-detalles-facturas-relacion-factura" author="kisser">

        <!-- Verificar si la columna factura_id ya existe -->
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="detalles_facturas" columnName="factura_id"/>
            </not>
        </preConditions>

        <!-- Agregamos la columna factura_id a detalles_facturas -->
        <addColumn tableName="detalles_facturas">
            <column name="factura_id" type="BIGINT"/>
        </addColumn>

        <!-- Hacemos la columna NOT NULL -->
        <addNotNullConstraint
            tableName="detalles_facturas"
            columnName="factura_id"
            columnDataType="BIGINT"/>

        <!-- Creamos la foreign key hacia facturas -->
        <addForeignKeyConstraint
            baseTableName="detalles_facturas"
            baseColumnNames="factura_id"
            referencedTableName="facturas"
            referencedColumnNames="id"
            constraintName="fk_detalles_facturas_facturas"/>
            
    </changeSet>
</databaseChangeLog>